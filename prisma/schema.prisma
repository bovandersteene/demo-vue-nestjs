datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Dog {
  id          Int    @id @default(autoincrement())
  name        String @unique
  age         Int
  totalTeeths Int?
}
